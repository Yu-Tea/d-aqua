<% content_for(:title, t('.title')) %>
<div class="container mx-auto h-screen">
  <div class="bg_subpage">
    <div class="text-center mx-5 sm:mx-10">
      <div class="text-center mx-auto max-w-sm">
        <h1><%= t('.title') %></h1>
        <%= form_with url: password_resets_path do |f| %>
          <div class="mt-8 mb-3 text-left">
            <%= f.label :email, class: "block font-bold main-color mb-1" %>
            <%= f.email_field :email, class: "appearance-none border rounded-md w-full py-2 px-4 text-gray-700 leading-tight focus:border-blue-400 focus:outline-none" %>
          </div>
          <button type="submit" class="aqua-button my-3"><i class="fa-solid fa-envelope mr-1"></i>送信</button>
        <% end %>
      </div>
    </div>
  </div>
</div>